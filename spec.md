# Captain Cloppy

## Current State
Full-stack trading dashboard for Odin.fun with: Dashboard (Trending Tokens + Mini Global Feed), Token Explorer (searchable/sortable grid with 24h % change), Trading Page (DEX-style buy/sell), Token Detail Modal (3 tabs: Chart, Overview, Feed), History Page (User Trades + Global Feed with LIVE/PAUSED), Bot Page, Sidebar navigation, mobile-optimized with bottom nav.

## Requested Changes (Diff)

### Add
- Nothing new

### Modify
- Rebuild/redeploy existing app as-is (draft expired)

### Remove
- Nothing

## Implementation Plan
1. Rebuild the frontend from existing source files
2. Validate and deploy
